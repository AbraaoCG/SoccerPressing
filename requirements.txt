# Dependências do projeto — Análise de Pressing (pressing_v1.ipynb)
# Instalação: pip install -r requirements.txt

# Manipulação e análise de dados
pandas>=2.0
numpy>=1.24

# Testes estatísticos e modelagem
scipy>=1.10
statsmodels>=0.14

# Geometria (diagramas de Voronoi)
shapely>=2.0

# Visualização
matplotlib>=3.7

# Classificação da geometria do pressing (classify_pressing.ipynb)
scikit-learn>=1.3      # k-means, PCA, regressão logística, métricas, MLP (fallback)
torch>=2.2             # GNN (opcional; sem torch o notebook usa fallback DeepSets via sklearn)

# Ambiente de execução do notebook
jupyterlab>=4.0
ipykernel>=6.0
